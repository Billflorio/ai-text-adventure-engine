# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.4
python-multipart>=0.0.9

# HTTP requests (Gutenberg API, model APIs)
requests>=2.32.0
httpx>=0.27.0

# LLM backends
openai>=1.30.0          # OpenAI + any OpenAI-compatible endpoint + LM Studio
anthropic>=0.28.0       # Anthropic Claude
google-generativeai>=0.7.0  # Google Gemini

# Book parsing
PyMuPDF>=1.24.0         # PDF (imported as fitz)
ebooklib>=0.18          # EPUB
beautifulsoup4>=4.12.0  # HTML cleanup for some epub formats
lxml>=5.2.0

# Utilities
pydantic>=2.7.0         # Data validation / structured AI output
python-dotenv>=1.0.0    # Optional .env support for API keys
Pillow>=10.3.0          # Image handling / resizing generated art
aiofiles>=23.2.0        # Async file I/O for FastAPI
